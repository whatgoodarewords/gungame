Role: Codex GPT-5.6 lane "hotfix4 — render resilience (WebGPU black screen P0)". Owner screenshot: on real-browser WebGPU, the world renders pure black (HUD/DOM fine, connection fine); frame capture hangs = no presentable frame. Headless WebGPU repro unavailable; fix the FAILURE CLASS, not just one bug. NORMATIVE: docs/SPEC.md §3.3, docs/art-direction.md perf/elegance clauses. You write all code; NO git ops; don't touch docs/, deploy/.

Deliverables:
1. NO BLACK TERMINAL STATE, guaranteed: the initial pipeline construction (RecoverableRenderPipeline seed) and every style's postChain get a hard fallback chain: full style → plain pass(scene, camera) no-post → plain + forced WebGL2 hint toast ("visual quality reduced"). Any async pipeline/TSL compile failure at STARTUP must land on a rendering fallback within 2 frames, log the real error via console.error AND surface it in the settings panel diagnostics row (so an owner screenshot diagnoses it).
2. Lighting can never be all-or-nothing: keep a dim safety fill light (0.15 intensity hemisphere) in every rig so a failed HDRI/PMREM env produces a dim-but-visible world, never black. Env/cubemap/KTX2 loads get .catch fallbacks to basic materials with a diagnostics note.
3. Audit the WebGPU-vs-WebGL2 divergences in the phase7 pipeline for likely black-frame causes and fix concretely: prefiltered-cubemap texture format/upload on WebGPU (the offline PMREM outputs — verify format compatibility, add runtime validation), ACES/bloom TSL chain node compatibility, logarithmicDepthBuffer only set on webgl2 (fine), any renderer.init()-ordering assumption. Add a headless WebGL2 pixel test asserting a rendered frame is NOT >90% dark on the greybox map at spawn (the assertion the sweeps lacked), wired into tools/e2e.
4. Whatever the actual root cause turns out to be in your reading of the code, fix it directly and name it in the report.
Done: pnpm -r typecheck && test green incl. the new dark-frame test; report names the most likely WebGPU root cause with file:line.
Report → client/HOTFIX4-REPORT.md progressively.
