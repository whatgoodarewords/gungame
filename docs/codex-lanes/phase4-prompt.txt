Role: Codex GPT-5.6 implementation lane for gungame Phase 4 — Content & polish, CODE half (issue #7; NORMATIVE: docs/SPEC.md §3.3, §3.6; docs/maps-brief.md for the two maps; docs/arsenal-ladder.md weapon identities). The Prime (Fable) owns all AESTHETIC VALUES — palettes, materials, post-chain parameters, audio selection, typography — and will direct/replace values after your mechanical implementation. You write the code and structure; use placeholder-but-coherent values where the Prime hasn't pinned them. NO git operations. Do not touch docs/, deploy/.

Deliverables:

1. RenderStyle layer (client): a RenderStyle interface = {materials(map), postChain(TSL), palette, fogLightRig} as ONE swappable unit; implementations: dev-grid (triplanar grid), ink-duotone (blue-noise temporally-stable dither + 2-color palette, judged-in-motion), toon-cel (quantized lighting + inverted-hull outlines), brutalist-approx (fog volumes + single emissive accent rig + concrete material). A dev-panel style switcher (extend the existing panel). Bake-off harness: ?style= query param + the switcher. WebGPU-first via TSL, WebGL2-safe fallback for each.
2. Maps: build Spire and Foundry PROGRAMMATICALLY per docs/maps-brief.md (Blender authoring is post-v1 polish; the generator approach shipped greybox and is repeatable) — exact angles/heights/spawn counts per the brief, through the existing pipeline (blob + validator green, incl. secrets geometry). Map selection: map follows mode (Spire=scoutz, Foundry=gungame) per spec.
3. Weapon viewmodels: simple stylized meshes per weapon (14 configs, shared silhouettes where honest per spec), first-person layer with equip/fire/rack animations (procedural transforms, no anim files), Goldie reload choreography, Deadeye/scout scope overlay (vignette + reticle, FOV lerp already exists).
4. Audio: Web Audio engine — positional (existing PositionalAudio path ok), per-weapon fire/impact sounds SYNTHESIZED procedurally (noise+envelope+filter recipes as code, tunable params — no external assets; the Prime will tune or replace), §3.6 set: damage-pitched hitmarkers, headshot ding, kill-confirm, AIRSHOT sting, footsteps/landing by material flag, wind-by-speed loop, Foundry gg-sigil jingle + Spire secret-room ambience hook.
5. HUD/menus final structure: name-entry screen (name → quickplay; Create-room flow w/ mode/ladder/gravity picker per spec §3.4; invite-link copy button), in-match HUD (health, tier/ladder, ammo where relevant, speed readout kept, killfeed, scoreboard Tab, damage direction), death/respawn, win/scoreboard-freeze screen, "server restarting" + "connection lost" states, version-mismatch force-reload wiring (the Phase 2 TODO seam).
6. Secrets: Spire ledge room (geometry + names-wall texture hook) + Foundry knifeable sigil (knife-hit trigger → jingle once per match, server-validated).
7. Tests: map validator green for both maps (spawn counts, kill volumes, secrets nodes); style-switch smoke (each style renders one frame headless where feasible or documented manual matrix); HUD state-machine unit tests (death/respawn/win/restart transitions); audio-engine param sanity (no NaN/clipping in synth recipes).

Done means: pnpm -r typecheck && pnpm -r test green; both maps validate; playground boots into Spire with ?style= working for all four styles; a bot match on Foundry completes with HUD/killfeed/scoreboard functioning.

Report: conclusion, evidence, caveats, next action. APPEND progressively to client/PHASE4-REPORT.md. Land order: RenderStyle layer → maps → HUD → viewmodels → audio → secrets.
