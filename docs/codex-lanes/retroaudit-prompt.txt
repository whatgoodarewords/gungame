Role: Codex GPT-5.6 AUDIT lane (xhigh): retroactive independent review of the Prime-direct hotfix2 commit (the commit titled "hotfix2 (Prime-direct...)" at or near HEAD — find it with git log). NORMATIVE context: docs/review-settled-tree.md (the findings it implements), docs/SPEC.md. READ-ONLY: make NO code changes, NO git operations; your deliverable is a report file.

Audit `git show <that commit>` hunk by hunk against the review's concrete fixes: (1) does each change implement the finding correctly and completely (edge cases: rebalanceBots recursion/ordering, expireHolds-during-shouldReap mutation safety, spawn scoring at 1 candidate, pull-at-tick input during death/menu states, peek() vs zoom/fire presentation semantics, disposal on style rollback path — is anything disposed that rollback then reuses?); (2) any NEW bug introduced; (3) anything the commit claims but does not do. Verdict PASS/REVISE with file:line findings and concrete fixes. Write the report to docs/codex-lanes/retroaudit-report.md (you may write ONLY that file).
